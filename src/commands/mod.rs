/// Module that represents the 'new' command
pub mod new_command;

/// Module that represents the 'init' command
pub mod init_command;

/// Struct that represents the posible errors a command could have
pub mod command_error;
